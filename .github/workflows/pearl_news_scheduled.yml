# Pearl News: scheduled run on self-hosted runner (for LM Studio / local Qwen).
# Use this when QWEN_BASE_URL is localhost — cloud runners cannot reach it.
# Copy this to Qwen-Agent or Qwen repo as .github/workflows/pearl_news_scheduled.yml
# See docs/PEARL_NEWS_OPTION_B_RUNBOOK.md.

name: Pearl News scheduled
concurrency:
  group: pearl-news-scheduled
  cancel-in-progress: false

on:
  schedule:
    # 3 runs per day, one per language:
    # 06:00 UTC → English, 10:00 UTC → Japanese, 14:00 UTC → Simplified Chinese
    - cron: '0 6 * * *'   # English
    - cron: '0 10 * * *'  # Japanese
    - cron: '0 14 * * *'  # Simplified Chinese
  workflow_dispatch:
    inputs:
      language:
        description: 'Language (en, ja, zh-cn, or all)'
        required: false
        default: 'en'
        type: choice
        options:
          - en
          - ja
          - zh-cn
          - all

jobs:
  pearl-news:
    runs-on: self-hosted
    timeout-minutes: 45
    env:
      WORDPRESS_SITE_URL: ${{ secrets.WORDPRESS_SITE_URL }}
      WORDPRESS_USERNAME: ${{ secrets.WORDPRESS_USERNAME }}
      WORDPRESS_APP_PASSWORD: ${{ secrets.WORDPRESS_APP_PASSWORD }}
      QWEN_BASE_URL: ${{ secrets.QWEN_BASE_URL }}
      QWEN_API_KEY: ${{ secrets.QWEN_API_KEY }}
      QWEN_MODEL: ${{ secrets.QWEN_MODEL }}
      # Schedule = no expand (reliable drafts). For manual expand run: use pearl_news_manual_expand.yml
      PEARL_NEWS_EXPAND: "false"
      PEARL_NEWS_LIMIT: "3"
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Determine language from schedule or input
        id: lang
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "language=${{ inputs.language || 'en' }}" >> "$GITHUB_OUTPUT"
          else
            # Map cron schedule to language by hour (UTC)
            HOUR=$(date -u +%H)
            case "$HOUR" in
              06) echo "language=en" >> "$GITHUB_OUTPUT" ;;
              10) echo "language=ja" >> "$GITHUB_OUTPUT" ;;
              14) echo "language=zh-cn" >> "$GITHUB_OUTPUT" ;;
              *)  echo "language=en" >> "$GITHUB_OUTPUT" ;;
            esac
          fi

      - name: Check Python
        run: |
          python3 --version
          python3 -m pip --version

      - name: Install Pearl News dependencies
        run: |
          python3 -m pip install feedparser pyyaml requests pytest openai

      - name: Preflight runner + LM health
        run: |
          set -euo pipefail
          echo "== LM Studio model check =="
          curl -fsS "$QWEN_BASE_URL/models" >/tmp/models.json
          cat /tmp/models.json | head -c 400; echo
          echo "== LM Studio warmup call =="
          curl -fsS --max-time 45 "$QWEN_BASE_URL/chat/completions" \
            -H 'Content-Type: application/json' \
            -H "Authorization: Bearer $QWEN_API_KEY" \
            -d "{\"model\":\"$QWEN_MODEL\",\"messages\":[{\"role\":\"user\",\"content\":\"ok\"}],\"max_tokens\":8,\"temperature\":0.0}" >/tmp/warmup.json
          python3 - <<'PY'
          import json
          data = json.load(open("/tmp/warmup.json"))
          if "error" in data:
            raise SystemExit(f"Warmup failed: {data['error']}")
          print("warmup_ok=true")
          PY

      - name: Validate WordPress env (if configured)
        run: |
          PYTHONPATH=. python3 - <<'PY'
          import os
          from pearl_news.publish.wordpress_client import _normalize_site_url
          raw = os.environ.get("WORDPRESS_SITE_URL", "").strip()
          if not raw:
              print("WORDPRESS_SITE_URL not set; skipping WP env validation.")
          else:
              normalized = _normalize_site_url(raw)
              print(f"WORDPRESS_SITE_URL normalized to: {normalized}")
          PY

      - name: Run Pearl News tests
        run: |
          PYTHONPATH=. python3 -m pytest \
            tests/test_pearl_news_quality_gates_minimal.py \
            tests/test_pearl_news_pipeline_e2e.py \
            -v

      - name: Run full pipeline (ingest → assemble → optional expand → QC)
        run: |
          LANG="${{ steps.lang.outputs.language }}"
          EXPAND_FLAG=""
          VALIDATE_FLAG=""
          if [ "$PEARL_NEWS_EXPAND" = "true" ]; then
            EXPAND_FLAG="--expand --validate"
          fi
          for attempt in 1 2; do
            echo "attempt=${attempt}"
            if [ "$LANG" = "all" ]; then
              python3 -m pearl_news.pipeline.run_article_pipeline \
                --feeds pearl_news/config/feeds.yaml \
                --out-dir artifacts/pearl_news/drafts \
                --limit "$PEARL_NEWS_LIMIT" \
                --language all \
                $EXPAND_FLAG && exit 0
            else
              python3 -m pearl_news.pipeline.run_article_pipeline \
                --feeds pearl_news/config/feeds.yaml \
                --out-dir "artifacts/pearl_news/drafts/$LANG" \
                --limit "$PEARL_NEWS_LIMIT" \
                --language "$LANG" \
                $EXPAND_FLAG && exit 0
            fi
            if [ "$attempt" -eq 2 ]; then
              echo "pipeline failed after retry"
              exit 1
            fi
            sleep 15
          done

      - name: Upload draft artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pearl_news_drafts_${{ steps.lang.outputs.language }}
          path: artifacts/pearl_news/drafts/
          retention-days: 7

      - name: Test WordPress integration (dry-run or post)
        run: |
          shopt -s nullglob
          first=$(ls -1 artifacts/pearl_news/drafts/*/article_*.json artifacts/pearl_news/drafts/article_*.json 2>/dev/null | head -1)
          if [ -z "$first" ]; then
            echo "No article_*.json found; skipping"
            exit 0
          fi
          if [ -z "$WORDPRESS_APP_PASSWORD" ]; then
            python3 scripts/pearl_news_post_to_wp.py --article "$first" --dry-run
          else
            python3 scripts/pearl_news_post_to_wp.py --article "$first" --status draft
          fi
        continue-on-error: true
