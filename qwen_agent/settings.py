# Settings for LLMs
DEFAULT_MAX_TOKEN: int = 6000  # It doesn't work yet

# Settings for agents
MAX_LLM_CALL_PER_RUN: int = 8

DEFAULT_WORKSPACE: str = 'workspace'

# Settings for RAG
DEFAULT_MAX_REF_TOKEN: int = 4000
DEFAULT_PARSER_PAGE_SIZE: int = 500
